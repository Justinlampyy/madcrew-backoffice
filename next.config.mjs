/** @type {import('next').NextConfig} */
const nextConfig = {
  // NIET: output: 'export'
  output: 'standalone', // of laat deze regel helemaal weg
  reactStrictMode: true
}
export default nextConfig
